# TODO

- [ ] Analyze the provided single-file HTML (inline JS + CSS) into a safe folder/file structure.
- [ ] Create files: `index.html`, `assets/js/app.js`, `assets/css/styles.css` while preserving all code exactly (no logic changes).
- [ ] Update `index.html` to reference the new external JS/CSS files.
- [ ] Ensure script order matches original dependencies (three, OrbitControls, three-globe, tween).
- [ ] Test locally by running a static server and validating no runtime errors.
- [ ] Keep a backup copy of the original HTML as `original.html`.

